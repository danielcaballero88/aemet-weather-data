class AemetClient {
  constructor(private apiKey: string) {}

  // async getWeatherData(location: string) {
  //   const url = `https://api.aemet.es/weather/${location}`;
  //   const queryParams = { apiKey: this.apiKey };
  //   return makeGetRequest(url, queryParams);
  // }
}
